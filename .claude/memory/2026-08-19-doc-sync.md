# Doc sync 2026-08-19

Range reviewed: 0200eea → 0f1fdb5

Graph findings:

- Added the `message_templates` Prisma model, migration, NestJS module, and authenticated CRUD endpoints.
- Added dashboard template state/client mapping and Zalo modal template rendering/editor.
- Added pure rendering and backend ownership tests.

Edits made:

- `ARCHITECTURE.md` — Postgres table map, API endpoint map, Zalo/contact section, and production table count — documented the new teacher-owned message templates and verified migration state.
